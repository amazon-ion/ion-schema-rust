[workspace]

members = [
    "ion-schema",
    "ion-schema-tests-runner",
    "wasm-schema-sandbox",
]

resolver = "2"

[workspace.dependencies]
ion-rs = { version = "1.0.0", features = ["experimental-reader-writer"] }
